# Security

Only the current active tab is accessed after user action. Webpage DOM and subtitle files are untrusted; subtitle text is rendered with `textContent`, and no remote scripts or `eval` are used. Report vulnerabilities through the project's configured issue tracker; no security contact is currently published.
